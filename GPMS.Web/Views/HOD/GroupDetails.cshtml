@model GPMS.Application.DTOs.ProjectGroupDetailDto
@using GPMS.Domain.Enums
@{
    ViewData["Title"] = "Group Details";
    Layout = "~/Views/Shared/_HODLayout.cshtml";
    
    var statusBadgeClass = Model.ProjectStatus switch {
        ProjectStatus.Active => "bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200",
        ProjectStatus.Draft => "bg-amber-100 dark:bg-amber-900 text-amber-800 dark:text-amber-200",
        ProjectStatus.Completed => "bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200",
        _ => "bg-slate-100 dark:bg-slate-800 text-slate-800 dark:text-slate-200"
    };
}

<div class="min-h-screen bg-slate-50 dark:bg-slate-950">
    <div class="p-6">
        <div class="max-w-6xl mx-auto space-y-6">
            <!-- Breadcrumb Navigation -->
            <nav class="flex items-center gap-2 text-sm mb-6">
                <a href="/HOD/Dashboard" class="text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-200 no-underline">GPMS</a>
                <span class="text-slate-400 dark:text-slate-600">/</span>
                <a href="/HOD/Groups" class="text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-200 no-underline">Groups</a>
                <span class="text-slate-400 dark:text-slate-600">/</span>
                <span class="text-slate-900 dark:text-slate-100 font-medium">Chi tiết nhóm</span>
            </nav>

            <!-- Group Header Section -->
            <div class="bg-white dark:bg-slate-900 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-800">
                <div class="flex items-start justify-between">
                    <div class="flex items-start gap-4">
                        <div class="w-16 h-16 rounded-lg bg-gradient-to-br from-blue-500 to-indigo-600 flex items-center justify-center text-white text-2xl font-bold">
                            @Model.GroupName
                        </div>
                        <div>
                            <div class="flex items-center gap-3">
                                <h1 class="text-2xl font-bold text-slate-900 dark:text-white">@Model.GroupName · @Model.ProjectCode</h1>
                                <span class="px-3 py-1 @statusBadgeClass text-xs font-semibold rounded-full uppercase">@Model.ProjectStatus</span>
                            </div>
                            <p class="text-slate-600 dark:text-slate-400 mt-2">Đề tài: @Model.ProjectName</p>
                        </div>
                    </div>
                    <div class="flex gap-3">
                        <button class="px-6 py-2 bg-slate-100 dark:bg-slate-800 text-slate-900 dark:text-white rounded-lg hover:bg-slate-200 dark:hover:bg-slate-700 font-medium transition shadow-sm border border-slate-200 dark:border-slate-700">
                            Edit Info
                        </button>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-3 gap-6">
                <!-- Left Column - Team & Supervisor -->
                <div class="col-span-2 space-y-6">
                    <!-- Team Members Section -->
                    <div class="bg-white dark:bg-slate-900 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-800">
                        <div class="flex items-center justify-between mb-6">
                            <div class="flex items-center gap-2">
                                <span class="material-symbols-outlined text-blue-500" style="font-size: 24px;">group</span>
                                <h2 class="text-lg font-semibold text-slate-900 dark:text-white">Team Members</h2>
                            </div>
                            <span class="px-3 py-1 bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300 text-sm font-medium rounded-full">@Model.Members.Count Students</span>
                        </div>

                        <div class="space-y-3">
                            @foreach (var member in Model.Members)
                            {
                                var initials = string.Concat(member.FullName.Split(' ').Take(2).Select(w => w.Length > 0 ? w[0].ToString().ToUpper() : ""));
                                <div class="flex items-center justify-between p-4 bg-slate-50 dark:bg-slate-800 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-700 transition">
                                    <div class="flex items-center gap-3">
                                        <div class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-400 to-blue-600 flex items-center justify-center text-white font-semibold text-sm">
                                            @initials
                                        </div>
                                        <div>
                                            <p class="font-semibold text-slate-900 dark:text-white">@member.FullName</p>
                                            <p class="text-xs text-slate-500 dark:text-slate-400">MSSV: @member.UserID | @member.Email</p>
                                        </div>
                                    </div>
                                    <span class="px-3 py-1 @(member.RoleInGroup == GroupRole.Leader ? "bg-yellow-100 dark:bg-yellow-900 text-yellow-800 dark:text-yellow-200" : "bg-slate-200 dark:bg-slate-700 text-slate-700 dark:text-slate-300") text-xs font-semibold rounded-full">@member.RoleInGroup</span>
                                </div>
                            }
                        </div>
                    </div>

                    <!-- Assigned Supervisor Section -->
                    <div class="bg-white dark:bg-slate-900 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-800">
                        <div class="flex items-center gap-2 mb-6">
                            <span class="material-symbols-outlined text-blue-500" style="font-size: 24px;">person_check</span>
                            <h2 class="text-lg font-semibold text-slate-900 dark:text-white">Assigned Supervisor</h2>
                        </div>

                        <div class="flex items-center gap-4 p-4 bg-slate-50 dark:bg-slate-800 rounded-lg">
                            <div class="w-14 h-14 rounded-lg bg-gradient-to-br from-slate-600 to-slate-800 flex items-center justify-center text-white font-semibold text-lg">
                                @(Model.SupervisorName?.Split(' ').LastOrDefault()?.Substring(0, 1) ?? "S")@(Model.SupervisorName?.Split(' ').FirstOrDefault()?.Substring(0, 1) ?? "P")
                            </div>
                            <div class="flex-1">
                                <p class="font-semibold text-slate-900 dark:text-white">@(Model.SupervisorName ?? "Chưa phân công")</p>
                                <p class="text-sm text-slate-600 dark:text-slate-400">Giảng viên hướng dẫn</p>
                            </div>
                            @if (!string.IsNullOrEmpty(Model.SupervisorName))
                            {
                                <div class="flex gap-2">
                                    <button class="px-4 py-2 bg-slate-200 dark:bg-slate-700 text-slate-900 dark:text-white rounded-lg hover:bg-slate-300 dark:hover:bg-slate-600 text-sm font-medium transition">
                                        Contact
                                    </button>
                                </div>
                            }
                        </div>
                    </div>

                    <!-- Submissions & Evaluations Section -->
                    <div class="bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-200 dark:border-slate-800 overflow-hidden">
                        <div class="flex border-b border-slate-200 dark:border-slate-800">
                            <button class="flex-1 px-6 py-4 text-sm font-medium text-slate-900 dark:text-white border-b-2 border-blue-500 bg-white dark:bg-slate-900 transition">
                                Submissions
                            </button>
                            <button class="flex-1 px-6 py-4 text-sm font-medium text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white border-b-2 border-transparent transition">
                                Evaluations
                            </button>
                            <button class="flex-1 px-6 py-4 text-sm font-medium text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white border-b-2 border-transparent transition">
                                Feedback
                            </button>
                        </div>

                        <div class="p-6">
                            <div class="overflow-x-auto">
                                <table class="w-full text-sm">
                                    <thead class="bg-slate-50 dark:bg-slate-800">
                                        <tr>
                                            <th class="text-left py-3 px-4 font-semibold text-slate-700 dark:text-slate-300">Document Name</th>
                                            <th class="text-left py-3 px-4 font-semibold text-slate-700 dark:text-slate-300">Round</th>
                                            <th class="text-left py-3 px-4 font-semibold text-slate-700 dark:text-slate-300">Submit Date</th>
                                            <th class="text-left py-3 px-4 font-semibold text-slate-700 dark:text-slate-300">Status</th>
                                            <th class="text-left py-3 px-4 font-semibold text-slate-700 dark:text-slate-300">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody class="divide-y divide-slate-200 dark:divide-slate-800">
                                        <tr>
                                            <td colspan="5" class="py-10 text-center text-slate-500 italic">Dữ liệu nộp bài đang được cập nhật...</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Right Column - Reviewers & Timeline -->
                <div class="space-y-6">
                    <!-- Reviewer Section -->
                    <div class="bg-white dark:bg-slate-900 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-800">
                        <div class="flex items-center gap-2 mb-6">
                            <span class="material-symbols-outlined text-blue-500" style="font-size: 24px;">assignment_ind</span>
                            <h2 class="text-lg font-semibold text-slate-900 dark:text-white">Reviewers Panel</h2>
                        </div>

                        <div class="space-y-4">
                            <div class="p-4 bg-slate-50 dark:bg-slate-800 rounded-lg border-l-4 border-slate-300">
                                <p class="text-sm text-slate-500 italic">Thông tin hội đồng chấm sẽ xuất hiện khi các vòng Review bắt đầu.</p>
                            </div>
                        </div>
                    </div>

                    <!-- Review Timeline Section -->
                    <div class="bg-white dark:bg-slate-900 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-800">
                        <div class="flex items-center gap-2 mb-6">
                            <span class="material-symbols-outlined text-blue-500" style="font-size: 24px;">timeline</span>
                            <h2 class="text-lg font-semibold text-slate-900 dark:text-white">Review Timeline</h2>
                        </div>

                        <div class="space-y-4">
                            <div class="relative pl-3">
                                <div class="absolute left-[17px] top-2 bottom-6 w-0.5 bg-slate-200 dark:bg-slate-800 z-0"></div>
                                
                                <div class="relative z-10 flex gap-4 pb-8">
                                    <div class="flex-shrink-0">
                                        <div class="w-2.5 h-2.5 rounded-full bg-slate-300 dark:bg-slate-700 ring-4 ring-white dark:ring-slate-900 shadow-sm mt-1 mb-1"></div>
                                    </div>
                                    <div>
                                        <p class="font-semibold text-slate-500 dark:text-slate-400 text-sm">Chưa có dữ lộ tiến độ</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
